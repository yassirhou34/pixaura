import type { ReactNode } from "react"

/** Routes vitrine marketing : header/footer gérés par page (Navbar, etc.) */
export default function MarketingLayout({ children }: { children: ReactNode }) {
  return children
}
